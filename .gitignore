.tmp/
materiais/
.github/scripts/node_modules/

# b140 — instalador do impeccable (npx impeccable install) espalha por .github/ e .claude/.
# Ferramenta de design local, não faz parte do produto. .claude/ já era untracked.
.github/agents/
.github/hooks/
.github/skills/

graphify-out/
.graphifyignore

# b146 — cópia-sombra do <script> embutido em index.html, gerada por
# materiais/entregas/testes/extrair_shadow_js.mjs, só pro Understand-Anything conseguir
# ler a lógica do app (ele não tem gramática HTML). Nunca é o código real.
.ua-shadow/
.ua/

# b165 — nunca versionar:
# - cesar-coin-gray.png: asset orfao, proibido pelo CLAUDE.md §8
# - hook.cache.json: cache do impeccable, com UUID de sessao e caminho absoluto local
# - .claude/: 14 MB de estado de sessao local, nao faz parte do produto
assets/cesar-coin-gray.png
.impeccable/hook.cache.json
.impeccable/critique/
.claude/

# b168 — PRODUCT.md/DESIGN.md/.impeccable/design.json continuam na raiz (a skill impeccable
# le esse caminho fixo) mas nunca publicos: posicionamento competitivo e detalhe de design
# system nao tem por que estar no repo publico. Mesmo padrao das linhas acima.
PRODUCT.md
DESIGN.md
.impeccable/design.json

# b223 — estado local da skill impeccable: config.json guarda supressoes com caminho e
# data local da maquina; live/ e sessao de anotacao. Mesmo motivo das linhas acima.
.impeccable/config.json
.impeccable/live/

# b218 — NUNCA versionar: o CLAUDE.md entrega a arquitetura, as armadilhas ja pagas,
# a estrategia comercial do §7 (precos e margem) e o §11 (lista do que e fraco) para
# qualquer um que clone o repo PUBLICO. Fica em disco, funciona igual (o Claude Code le
# do disco, nao do GitHub). Segunda via: copia no vault, que sincroniza com o Drive.
CLAUDE.md

# b220 (PS1) — o banco de conteudo sai do repo publico. Fica na raiz, gitignorado; o
# cliente busca via carregarConteudo() no RTDB (no "conteudo", atras do login), publicado
# por materiais/entregas/publicacao/publicar_conteudo.js. Continua em disco, todas as
# ferramentas de auditoria/edicao seguem achando o arquivo igual.
# Ver materiais/entregas/planos/PLANO_conteudo_pos_login.md.
data.js
